% -------------------------------------------------------------
% PHẦN IV: AI PIPELINE (models/moi/)
% -------------------------------------------------------------
\section{AI Pipeline}

\begin{frame}{16. Kiến trúc Tổng thể (Proposed Architecture)}
    \begin{enumerate}
        \item \textbf{Biểu diễn Dữ liệu (Data Representation):} Chuyển đổi cấu trúc dự án thành Đồ thị Dị thể (Heterogeneous Graph) liên kết 3 tập đỉnh thực thể: Task (Công việc), Resource (Tài nguyên) và Shift (Ca/Lịch làm việc).
        \item \textbf{Học Đặc trưng (Feature Learning):} Ứng dụng mạng nơ-ron đồ thị (HGT) để ngoại suy các tham số nhiễu (noise parameters) bao gồm độ trễ kỳ vọng và phương sai.
        \item \textbf{Mô phỏng Ngẫu nhiên (Stochastic Simulation):} Sử dụng phương pháp Monte Carlo tích hợp phân phối Beta-PERT nhằm định lượng Xác suất Tới hạn (Criticality Index - $CI$).
        \item \textbf{Xây dựng Không gian Trạng thái (State Space Formulation):} Khởi tạo các kịch bản thực thi (Execution Modes) dựa trên phân tích tương quan (Attention Scores) và $CI$.
        \item \textbf{Tối ưu hóa Ràng buộc (Constraint Optimization):} Giải bài toán quy hoạch thỏa mãn ràng buộc (CP-SAT) nhằm xác định tập hợp nghiệm đánh đổi (Trade-off Solutions).
    \end{enumerate}
\end{frame}

\begin{frame}{16b. Kiến trúc chi tiết HGT}
    \centering
    \includegraphics[width=\textwidth, height=0.85\textheight, keepaspectratio]{fig2_hgt_architecture.drawio.png}
\end{frame}

\begin{frame}{17. Huấn luyện Tiền kỳ (Pre-training Strategy)}
    \begin{itemize}
        \item \textbf{Cơ chế Khôi phục (Masked Autoencoder):} Che khuất ngẫu nhiên (Masking) $25\%$ đặc trưng của các nút công việc (Task Nodes) nhằm kích thích mô hình tự học biểu diễn.
        \item \textbf{Mục tiêu Tối ưu:} Cực tiểu hóa hàm suy hao \textbf{Smooth L1 Loss} đối với vector đặc trưng được tái tạo (\texttt{reconstructed\_x}).
        \item \textbf{Mục đích:} Nâng cao khả năng khái quát hóa (generalization) trước khi tinh chỉnh (fine-tuning) trên tập dữ liệu nhỏ.
    \end{itemize}
    \centering
    \vspace{0.1cm}
    \includegraphics[width=0.9\textwidth, height=0.4\textheight, keepaspectratio]{fig7_pretraining_flow.drawio.png}
\end{frame}

\begin{frame}{17b. Kiến trúc Transfer Learning (Pre-training \& Fine-tuning)}
    \centering
    \includegraphics[width=\textwidth, height=0.75\textheight, keepaspectratio]{fig8_pretrain_finetune.drawio.png}
\end{frame}

\begin{frame}{18. Suy luận và Khai phá Tương quan (Inference \& Attention)}
    \begin{columns}
        \column{0.5\textwidth}
        \textbf{Cấu trúc Suy luận (Inference Module):}
        \begin{itemize}
            \item \texttt{duration\_factor}: Hàm kích hoạt Softplus kết hợp dịch chuyển $0.5$ nhằm đảm bảo tính dương nghiêm ngặt.
            \item \texttt{expected\_delay}: Hàm kích hoạt ReLU loại trừ nhiễu âm.
            \item \texttt{uncertainty\_sigma}: Ước lượng phương sai thông qua hàm Softplus $+ 0.01$.
        \end{itemize}
        
        \column{0.5\textwidth}
        \textbf{Cơ chế Tập trung Tương tự Cosine (Cosine Attention):}
        Định lượng mức độ phụ thuộc đa thực thể (bottleneck) giữa tập Công việc ($T$), Tài nguyên ($R$) và Ca kíp ($S$).
        \begin{equation}
            \text{sim}(t, \{r, s\}) = \frac{\mathbf{h}_t \cdot \mathbf{h}_{\{r, s\}}}{\|\mathbf{h}_t\| \|\mathbf{h}_{\{r, s\}}\|}
        \end{equation}
    \end{columns}
\end{frame}

\begin{frame}{18b. Cơ chế Truyền thông điệp \& Tập trung (Message Passing \& Attention)}
    \centering
    \includegraphics[width=\textwidth, height=0.75\textheight, keepaspectratio]{fig4_hgtconv_attention.drawio.png}
\end{frame}

\begin{frame}{18. B4: Mô phỏng Ngẫu nhiên (Stochastic CPM)}
    \textbf{Tích hợp tham số nhiễu ngoại suy vào phân phối Beta-PERT:}
    Tiến trình lấy mẫu ngẫu nhiên (Sampling Process) cho từng công việc qua $N$ vòng lặp:
    \begin{align}
        m &= \text{duration} \times \text{duration\_factor} + \text{expected\_delay} \\
        a &= \max(0.5, \; m \times (1 - 2\sigma)) \\
        b &= m \times (1 + 3\sigma)
    \end{align}
    
    \vspace{0.2cm}
    \textbf{Tổng hợp Phân phối Thống kê Cốt lõi:}
    \begin{itemize}
        \item $P_{50}, P_{80}, P_{95}$: Các mốc thời gian hoàn thành dựa trên độ tin cậy.
        \item \textbf{Criticality Index ($CI_i$):} Xác suất nút $i$ định vị trên đường găng (Khác biệt so với cơ chế Đường găng tĩnh của các phần mềm truyền thống).
    \end{itemize}
    \textit{Vai trò:} Tích số $CI \times \text{Duration}$ tạo thành \textbf{Total Risk}, đóng vai trò heuristic định hướng, hỗ trợ bộ giải CP-SAT tập trung ngân sách tối ưu vào đúng các nút thắt cổ chai (bottlenecks).
\end{frame}

\begin{frame}{19. B5: Tối ưu hóa Lịch trình (CP-SAT Optimizer)}
    \textbf{Chiến lược Rút ngắn Tiến độ Dẫn hướng (Heuristic-Guided Crashing):}
    Cơ chế này giới hạn không gian tìm kiếm thông qua việc chỉ áp dụng các phương án nén thời gian (Crashing) lên các nút mạng có $CI$ vượt ngưỡng (threshold), qua đó tối ưu hóa chi phí biên (marginal cost).
    
    \vspace{0.2cm}
    \textbf{Định hình Không gian Giải pháp (Alternative Execution Modes):}
    \begin{table}
        \centering
        \footnotesize
        \begin{tabular}{@{}llcc@{}}
            \toprule
            \textbf{Mode} & \textbf{Chiến lược} & \textbf{Thời gian} & \textbf{Chi phí} \\
            \midrule
            \textbf{0. Normal} & Giữ nguyên thiết kế ban đầu. & 100\% & 100\% \\
            \textbf{1. Overtime} & Khai thác ngoài giờ (CI > 0). & $\sim$66\% & Động (Theo Từ điển Tài nguyên) \\
            \textbf{2. AddRes} & Bổ sung tài nguyên ngoài. & $\sim$70\% & + Chi phí cấu thành thuê ngoài \\
            \textbf{3. Hybrid} & Phối hợp tối đa hóa nguồn lực. & $\sim$50\% & + Chi phí cận biên cao nhất \\
            \bottomrule
        \end{tabular}
    \end{table}
\end{frame}

\begin{frame}{20. B5: Cấu trúc Mô hình Quy hoạch (Constraint Formulation)}
    \textbf{Mô hình Toán học (CP-SAT):}
    \begin{enumerate}
        \item \textbf{Decision Variables:} Biến `start` (Miền liên tục) và `Optional Interval` (Biến Boolean 0/1) phục vụ lựa chọn Mode thi công.
        \item \textbf{Precedence \& Cumulative:} Áp dụng Lan truyền ràng buộc (Domain Propagation) tuân thủ logic mạng lưới và năng lực tài nguyên.
        \item \textbf{Search Mechanism:} Tích hợp Rẽ nhánh (Branching) và Học lỗi (Clause Learning/Backtracking) nhằm tối ưu không gian tìm kiếm.
        \item \textbf{Grid Sweep Pareto:} Trượt hàm mục tiêu $Z = \alpha \cdot T + \beta \cdot C + \gamma \cdot R$ để ngoại suy phổ phương án tối ưu.
    \end{enumerate}
    \vspace{0.5cm}
    \textit{(Áp dụng bộ giải Google OR-Tools \cite{ortools} và phương pháp \cite{ga2023})}
\end{frame}

\begin{frame}{20b. Sơ đồ Luồng xử lý CP-SAT Pareto}
    \centering
    \includegraphics[width=\textwidth, height=0.75\textheight, keepaspectratio]{fig6_cpsat_pareto.drawio.png}
\end{frame}
